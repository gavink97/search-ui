version: "3"
services:

  next-dev:
    image: gavink97/next:dev
    build:
      context: ${PWD}/next
      dockerfile: Dockerfile

  pyapp:
    image: gavink97/pyapp:latest
    build:
      context: ${PWD}/webscraper
      dockerfile: Dockerfile
